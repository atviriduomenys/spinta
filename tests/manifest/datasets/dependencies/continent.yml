type: model
name: datasets/dependencies/Continent
external:
  dataset: datasets/dependencies
  resource: continents
  name: /continents.csv
  pk: id
properties:
  id:
    type: string
    external: id
  title:
    type: string
    external: continent
  continent_id:
    type: string
    external: id
  population:
    type: integer
    external: population
