type: model
name: datasets/generator/Continent
external:
  dataset: datasets/generator
  resource: continents
  name: continents.csv
  pk: id
  params:
    - year: range(2000, 2001)
properties:
  id:
    type: string
    external: id
  title:
    type: string
    external: continent
